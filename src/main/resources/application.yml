spring:
  profiles:
    active: local
  application:
    name: scankorea
  jpa:
    show-sql: true
    database: mysql
    open-in-view: false
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/SCANKOREA?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password: qwer!0842
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB

logging:
  level:
    org.hibernate:
      orm.jdbc.bind: trace
      SQL: DEBUG
